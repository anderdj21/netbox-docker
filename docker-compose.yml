services:
  netbox: &netbox
    image: docker.io/netboxcommunity/netbox:${VERSION-v4.6-5.0.1}
    depends_on:
      - postgres
      - redis
      - redis-cache
    env_file: env/netbox.env
    user: "netbox:root"
    healthcheck:
      test: curl -f http://localhost:8080/login/ || exit 1
      start_period: 90s
      timeout: 3s
      interval: 15s
    ports:
      - "23100:8080"
    networks:
      lamp-network:
        ipv4_address: 172.20.0.20
    volumes:
      - ./configuration:/etc/netbox/config:z,ro
      - netbox-media-files:/opt/netbox/netbox/media:rw
      - netbox-reports-files:/opt/netbox/netbox/reports:rw
      - netbox-scripts-files:/opt/netbox/netbox/scripts:rw

  netbox-worker:
    <<: *netbox
    depends_on:
      netbox:
        condition: service_healthy
    command:
      - /opt/netbox/venv/bin/python
      - /opt/netbox/netbox/manage.py
      - rqworker
    healthcheck:
      test: ps -aux | grep -v grep | grep -q rqworker || exit 1
      start_period: 20s
      timeout: 3s
      interval: 15s
    ports: []
    networks:
      - lamp-network

  # postgres
  postgres:
    image: docker.io/postgres:16-alpine
    healthcheck:
      test: pg_isready -q -t 2 -d $$POSTGRES_DB -U $$POSTGRES_USER
      start_period: 20s
      timeout: 30s
      interval: 10s
      retries: 5
    env_file: env/postgres.env
    networks:
      - lamp-network
    volumes:
      - netbox-postgres:/var/lib/postgresql

  # redis
  redis:
    image: docker.io/valkey/valkey:9.0-alpine
    command:
      - sh
      - -c 
      - valkey-server --appendonly yes --requirepass $$REDIS_PASSWORD 
    healthcheck: &redis-healthcheck
      test: '[ $$(valkey-cli --pass "$${REDIS_PASSWORD}" ping) = ''PONG'' ]'
      start_period: 5s
      timeout: 3s
      interval: 1s
      retries: 5
    env_file: env/redis.env
    networks:
      - lamp-network
    volumes:
      - netbox-redis-data:/data

  redis-cache:
    image: docker.io/valkey/valkey:9.0-alpine
    command:
      - sh
      - -c 
      - valkey-server --requirepass $$REDIS_PASSWORD 
    healthcheck: *redis-healthcheck
    env_file: env/redis-cache.env
    networks:
      - lamp-network
    volumes:
      - netbox-redis-cache-data:/data

networks:
  lamp-network:
    external: true

volumes:
  netbox-media-files:
  netbox-postgres:
  netbox-redis-cache-data:
  netbox-redis-data:
  netbox-reports-files:
  netbox-scripts-files:

# Y en la sección de servicios, vinculamos las rutas físicas directamente:
services:
  netbox:
    # ... (todo igual)
    volumes:
      - ./configuration:/etc/netbox/config:z,ro
      - /opt/netbox-docker/data/media:/opt/netbox/netbox/media:rw
      - /opt/netbox-docker/data/reports:/opt/netbox/netbox/reports:rw
      - /opt/netbox-docker/data/scripts:/opt/netbox/netbox/scripts:rw

  postgres:
    # ... (todo igual)
    volumes:
      - /opt/netbox-docker/data/postgres:/var/lib/postgresql/data

  redis:
    # ... (todo igual)
    volumes:
      - /opt/netbox-docker/data/redis-data:/data

  redis-cache:
    # ... (todo igual)
    volumes:
      - /opt/netbox-docker/data/redis-cache-data:/data
